<IconButton>:
    canvas:
        Color:
            rgba: self.icon_color
        Rectangle:
            size: min(*self.size)-self.padding[0]*2, min(*self.size)-self.padding[0]*2
            pos: self.pos[0]+self.padding[0], self.pos[1]+self.padding[1]
            texture: self.icon
